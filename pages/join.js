import Header from "../compornents/Header/joinHeader"
import Form from "../compornents/Form/joinFrom"

const join = () => {
    return (
        <div>
            <Header></Header>
            <Form></Form>
        </div>
    );
};

export default join;
